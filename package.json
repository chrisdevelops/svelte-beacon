{
  "name": "svelte-beacon",
  "version": "0.0.1",
  "description": "Contextual feedback collection and AI-assisted resolution for SvelteKit applications",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/widget/index.d.ts",
      "svelte": "./dist/widget/index.js",
      "default": "./dist/widget/index.js"
    },
    "./server": {
      "types": "./dist/server/index.d.ts",
      "default": "./dist/server/index.js"
    }
  },
  "bin": {
    "beacon": "./cli/index.js"
  },
  "files": [
    "dist/",
    "cli/"
  ],
  "scripts": {
    "build": "npm run build:server && npm run build:widget && npm run build:dashboard",
    "build:server": "tsc -p tsconfig.server.json",
    "build:widget": "svelte-package -i src/widget -o dist/widget",
    "build:dashboard": "cd dashboard && npm run build",
    "dev:dashboard": "cd dashboard && npm run dev",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "cd e2e && npx playwright test",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "svelte": "^5.0.0",
    "@sveltejs/kit": "^2.0.0"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/package": "^2.3.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@testing-library/svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
